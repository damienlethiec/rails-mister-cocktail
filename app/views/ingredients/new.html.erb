<%= simple_form_for(@ingredient) do |f| %>

  <%= f.input :name %>
  <%= f.input :photo, as: :attachinary %>

  <%= f.button :submit %>
<% end %>

<%= link_to 'Last cocktail visited', url_for(:back) %>
